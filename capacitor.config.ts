import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.starbella.catalogo',
    appName: 'Star Bella',
    webDir: 'dist',
};

export default config;
